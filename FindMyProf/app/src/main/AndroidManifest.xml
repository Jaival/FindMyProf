<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jaivalsaija.findmyprof">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Professor.HistoryProfessor"></activity>
        <activity android:name=".Professor.NotifyProfessor" />
        <activity android:name=".Student.HistoryStudent" />
        <activity android:name=".Student.NotifyStudent" />
        <activity android:name=".Student.Request" />
        <activity android:name=".Professor.ProfessorDashboard" />
        <activity android:name=".Student.StudentDashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>